curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
rustup update

cargo install fd-find # maybe alias to fd
cargo install taplo-cli --locked
cargo install eza
cargo install zoxide --locked
cargo install --locked yazi-fm yazi-cli
